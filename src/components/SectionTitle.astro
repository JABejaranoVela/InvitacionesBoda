---
const { eyebrow = "", title, subtitle = "" } = Astro.props;
---
<div class="text-center">
  {eyebrow && <p class="text-xs uppercase tracking-[0.4em] text-ink-soft">{eyebrow}</p>}
  <h2 class="mt-3 font-display text-3xl text-ink md:text-4xl">{title}</h2>
  {subtitle && <p class="mx-auto mt-4 max-w-2xl text-sm text-ink-soft md:text-base">{subtitle}</p>}
</div>
