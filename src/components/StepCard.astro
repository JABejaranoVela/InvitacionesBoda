---
const { step, title, description } = Astro.props;
---
<div class="rounded-3xl border border-sand bg-paper-strong p-6">
  <p class="text-xs uppercase tracking-[0.4em] text-ink-soft">Paso {step}</p>
  <h3 class="mt-3 font-display text-2xl">{title}</h3>
  <p class="mt-2 text-sm text-ink-soft">{description}</p>
</div>
