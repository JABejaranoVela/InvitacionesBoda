---
const { variant = "solid" } = Astro.props;
const isOverlay = variant === "overlay";
const navClass = isOverlay
  ? "mx-auto flex max-w-6xl items-center justify-between px-6 py-5 text-sm uppercase tracking-[0.2em] text-ink"
  : "mx-auto flex max-w-6xl items-center justify-between px-6 py-5 text-sm uppercase tracking-[0.2em] text-ink";
---
<header class={isOverlay ? "absolute inset-x-0 top-0 z-20" : "sticky top-0 z-30 bg-paper/95 backdrop-blur border-b border-sand"}>
  <nav class={navClass}>
    <a href="/" class="font-display text-lg normal-case tracking-wide">Logo</a>
    <div class="hidden items-center gap-8 md:flex">
      <a href="/" class={isOverlay ? "hover:text-accent transition" : "hover:text-accent transition"}>Inicio</a>
      <a href="/templates" class={isOverlay ? "hover:text-accent transition" : "hover:text-accent transition"}>Plantillas</a>
      <a href="/faq" class={isOverlay ? "hover:text-accent transition" : "hover:text-accent transition"}>Preguntas</a>
      <a href="/contacto" class={isOverlay ? "hover:text-accent transition" : "hover:text-accent transition"}>Contacto</a>
      <a href="/checkout" class={isOverlay ? "rounded-full bg-olive px-5 py-2 text-xs font-semibold tracking-[0.2em] text-white transition hover:bg-olive-soft" : "rounded-full bg-olive px-5 py-2 text-xs font-semibold tracking-[0.2em] text-white transition hover:bg-olive-soft"}>Ordenar</a>
    </div>
    <button class={isOverlay ? "md:hidden rounded-full border border-ink/40 px-4 py-2 text-xs font-semibold tracking-[0.2em] text-ink" : "md:hidden rounded-full border border-sand px-4 py-2 text-xs font-semibold tracking-[0.2em]"}>Menú</button>
  </nav>
</header>
